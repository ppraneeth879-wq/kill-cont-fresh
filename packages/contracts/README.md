# Contracts Package Plan

This package will eventually store shared contracts such as:

- API schemas
- shared enums
- incident status values
- asset type values
- generated docs if needed

Why it matters:

- it keeps frontend and backend terminology aligned.
