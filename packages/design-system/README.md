# Design System Plan

This package is reserved for shared design tokens and reusable UI primitives.

Expected future contents:

- color tokens
- spacing tokens
- motion tokens
- status colors
- typography scale
- reusable badge and panel patterns

The visual language should support sports first, but remain reusable for other media categories later.
